package entity;

import entity.TestUtil;
import entity.EntityToRecord(entity.Account);

public class Test {
  public static void main(String[] args) {
     Account acc1 = new Account(1, 30000, "xxxx");
     Account acc2 = acc1.setBalance(20000);
     TestUtil.testEqual(30000, acc1.getBalance());
     TestUtil.testEqual(20000, acc2.getBalance());
  }
}

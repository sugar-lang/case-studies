package entity;

import entity.TestUtil;

import entity.EntityToSQL(entity.Account);
import entity.EntityToSQL(entity.Customer);

public class TestCustomerSQL {
  public static void main(String[] args) {
     Account acc = new Account(1, 30000, "xxxx");
     Customer jane = new Customer("Jane", "Springfield", acc);
     
     TestUtil.testEqual(30000, jane.getAccount().getBalance());
  }
}


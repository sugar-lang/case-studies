package entity;

import entity.Account;
import java.lang.String;

public model Customer instantiates Entity {
  [Tuple(["name", "String"])
  ,Tuple(["address", "String"])
  ,Tuple(["account", "Account"])] 
}